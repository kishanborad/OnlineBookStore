<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_register"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorBase"
    tools:context="com.example.aishwarya.onlinebookstore.RegisterActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:orientation="vertical"
        android:padding="20dp">

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter fullname"
            android:id="@+id/xFullname"
            android:layout_centerHorizontal="true"
            android:padding="10dp"
            android:background="@color/colorWhite"
            android:textSize="20dp"
            android:drawableLeft="@drawable/fullname_small"
            android:drawablePadding="10dp"
            />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter username"
            android:id="@+id/xUsername"
            android:layout_centerHorizontal="true"
            android:padding="10dp"
            android:background="@color/colorWhite"
            android:textSize="20dp"
            android:drawableLeft="@drawable/user_small"
            android:drawablePadding="10dp"
            android:layout_marginTop="20dp"
        />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter password"
        android:id="@+id/xPassword"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/xUsername"
        android:padding="10dp"
        android:background="@color/colorWhite"
        android:textSize="20dp"
        android:drawableLeft="@drawable/lock_small"
        android:drawablePadding="10dp"
        android:inputType="numberPassword"
        android:fontFamily="sans-serif"
        android:layout_marginTop="20dp"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Register"
        android:background="@drawable/button_background"
        android:layout_below="@+id/xPassword"
        android:id="@+id/xRegister"
        android:layout_marginTop="20dp"
        />

    </LinearLayout>
</RelativeLayout>
